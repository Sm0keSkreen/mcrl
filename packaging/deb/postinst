#!/bin/sh
set -e

echo ""
echo "mcrl.jar is installed at /usr/share/mcrl/mcrl.jar"
echo "One-time setup still needed: point JDK_JAVA_OPTIONS at that path (see the mcrl"
echo "README)."
echo "Want the Realms/telemetry/profanity extras? Writes config.json right next to this"
echo "jar, no separate download or install directory (needs sudo, /usr/share/mcrl is a"
echo "system directory):"
echo "  curl -fsSL https://github.com/Sm0keSkreen/mcrl/releases/latest/download/Mcrl.sh -o /tmp/Mcrl.sh"
echo "  sudo bash /tmp/Mcrl.sh --configure-only /usr/share/mcrl"

exit 0
